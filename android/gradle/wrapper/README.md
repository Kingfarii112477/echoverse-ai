# Gradle Wrapper

The `gradle-wrapper.jar` should be downloaded by running:
```bash
cd android && ./gradlew wrapper
```

Or manually download from: https://services.gradle.org/distributions/gradle-8.2-bin.zip
